# Project Handover Context

This folder (`daily-math-v2`) is a duplicate of the original `daily-math` app (from the `sonic-filament` directory). It was cloned exactly after implementing two major features:
1. **Math Problem Diversity:** The AI now generates problems from 15 different math branches (~60 subtopics) instead of just Calculus.
2. **Adaptive Keyboard System:** The `ScientificKeyboard` component now features 6 distinct modes (`NUM`, `ALG`, `CALC`, `TRIG`, `LOGIC`, `FRAC`, `GEO`) and automatically switches layout based on the `answerType` of the math problem generated by AI. 

We also added geometry problem support (handling area, triangles, angles) and normalized validation for symbolic text matching.

## Goal
The user branched this codebase because they "want to start working on a slightly different version of the app." 

*Note to AI Agent: You are in a new chat thread now. Treat this folder as the root workspace.*
